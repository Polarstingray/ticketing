fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
itsdangerous==2.2.0
pydantic[email]==2.10.4
python-multipart==0.0.20
slowapi==0.1.9
# Runtime HTTP client for two independent callers: the chat assistant's
# provider calls (backend/chat/provider.py) and the webhook dispatcher
# (dispatcher.py). Also what starlette's TestClient uses, so it was previously
# pulled in by requirements-dev.txt only.
httpx==0.28.1
