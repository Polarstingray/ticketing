fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
itsdangerous==2.2.0
pydantic[email]==2.10.4
python-multipart==0.0.20
slowapi==0.1.9
# The webhook dispatcher's HTTP client (dispatcher.py). A runtime dependency,
# not just a test one — it was previously pulled in only by the TestClient.
httpx==0.28.1
