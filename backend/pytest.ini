[pytest]
# Keep rootdir inside the backend tree. Without this, pytest's config discovery
# walks ancestor directories up to /home (an autofs mount in some dev sandboxes)
# and stalls on a stat of a nonexistent parent config. testpaths also scopes
# collection to this directory.
testpaths = .
addopts = -p no:cacheprovider
