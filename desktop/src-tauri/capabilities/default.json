{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for all app windows.",
  "windows": ["*"],
  "permissions": ["core:default", "opener:default"]
}
