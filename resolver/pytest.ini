[pytest]
# Keep rootdir inside the resolver tree so pytest's config discovery never walks
# up to /home (an autofs mount in some dev sandboxes) and stalls before
# collection. See tests/conftest.py for the sys.path shim.
testpaths = tests
addopts = -p no:cacheprovider
