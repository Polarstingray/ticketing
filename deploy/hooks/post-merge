#!/usr/bin/env bash
# Auto-deploy after a merge or a pull — the moment a feature branch actually
# lands on main. Same detach rationale as post-commit.
REPO_ROOT="$(git rev-parse --show-toplevel)"
setsid nohup "$REPO_ROOT/deploy/autodeploy.sh" post-merge >/dev/null 2>&1 &
exit 0
