[pytest]
# Keep rootdir inside the cli tree so pytest's config discovery never walks up to
# /home (an autofs mount in some dev sandboxes) and stalls before collection.
# Same rationale as resolver/pytest.ini.
testpaths = tests
addopts = -p no:cacheprovider
