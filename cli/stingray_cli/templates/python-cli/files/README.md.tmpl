# {{project_name}}

{{description}}

## Status

Scaffolded. The interesting functions are stubs marked `STINGRAY-STUB:` — each
has a ticket. See `CLAUDE.md` for the convention.

## Develop

```bash
pip install -e .
{{project_name}} --help
python -m pytest
```
